namespace CustomerSupport.Shared.Enums;

public enum UserRole
{
    User = 1,
    Admin = 2
}

public enum TicketPriority
{
    Low = 1,
    Medium = 2,
    High = 3
}

public enum TicketStatus
{
    Open = 1,
    InProgress = 2,
    Closed = 3
}
